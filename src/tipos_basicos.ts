// Função multiplicadora
function multiplicadora(a:number, b:number):number{
    return a * b
}

// Função Saudação
const saudacao =  (nome:string): string => `Olá ${nome}`

console.log(multiplicadora(2, 4))
console.log(saudacao('matheus'))